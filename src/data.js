// estas funciones son de ejemplo

//export const example = () => {
  //return 'example';
//};

//export const anotherExample = () => {
  //return 'OMG';
//};

/*export const nameChampion = (nameOfChampion) => {
  let key = ""
  data[key]
  data.filter( function(name)){
  return data.name === 'Aatrox'
  
}
console.log(Aatrox);*/

/*function filterData(data, condition) {
  ctx.fillStyle = color;
  ctx.fillRect();
}*/






/*Ejemplo
square.js — contiene:
name — una constante que contiene la cadena «cuadrado».
draw() — dibuja un cuadrado en un lienzo específico, con un tamaño, posición y color especificados. Devuelve un objeto que contiene el tamaño, la posición y el color del cuadrado.


export const name = "square";

export function draw(ctx, length, x, y, color) {
  ctx.fillStyle = color;
  ctx.fillRect(x, y, length, length);

  return { length, x, y, color };*/
